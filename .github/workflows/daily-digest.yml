name: Weekly Recipe Digest Email

on:
  schedule:
    - cron: '0 14 * * 1'  # 9:00 AM EST every Monday
  workflow_dispatch:

jobs:
  send-digest:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Send weekly recipe digest
        env:
          MAILERLITE_API_KEY: ${{ secrets.MAILERLITE_API_KEY }}
        run: |
          echo "📧 Sending weekly recipe digest..."
          node scripts/send-daily-digest.js 2>&1
